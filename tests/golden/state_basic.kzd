// Basic state diagram: traffic light
state state_basic {
  // states
  state idle: "Idle"
  state active: "Active"
  state done: "Done"

  // transitions
  [*] -> idle
  idle -> active : "start"
  active -> active : "tick"
  active -> done : "finish"
  done -> [*]
}
